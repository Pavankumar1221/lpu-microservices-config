server:
  port: 7079

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springdb
    username: pavan
    password: Pavan@2004

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
